@../firstmate/AGENTS.md

<!-- Firstmate operational context for this project -->
FM_HOME is the `firstmate/` directory at `../firstmate/` (sibling of this project).
All operational state — `state/`, `data/`, `config/`, `projects/` — lives at `../firstmate/`.
All scripts (`bin/`) live at `../firstmate/bin/`.
This project (skill-eye) is the active development project managed by this firstmate instance.
